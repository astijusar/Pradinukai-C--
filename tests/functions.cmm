func forloop(x):
    if(x < 1):
        return;
    endif
    out(x);
    x = forloop(x = x - 1);
endfunc

func add(x, y):
    return x + y;
endfunc

func addThree(x, y, z):
    return x + y + z;
endfunc

//res = forloop(7);
res = addThree(1, 3, 5);
out(res);